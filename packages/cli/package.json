{
  "name": "@launchpad/cli",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "launchpad": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@launchpad/shared": "workspace:*",
    "@launchpad/guards": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "tsup": "^8.5.0"
  }
}
